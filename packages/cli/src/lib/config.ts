// Re-export everything from the shared @orfc/api package
export {
  loadConfig,
  saveConfig,
  getConfigDir,
  getConfigValue,
  setConfigValue,
} from "@orfc/api";
export type { OrfcConfig } from "@orfc/api";
